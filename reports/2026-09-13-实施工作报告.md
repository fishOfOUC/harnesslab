# HarnessLab 实施工作报告

- 报告日期：2026-09-13
- 工作范围：把 `docs/01`～`docs/11` 的设计文档落地为可运行、可验证的代码
- 交付阶段：**M0（环境与契约）+ M1（最小问答闭环）完成，M2（运行控制内核）主体完成**，M3～M6 未开始
- 代码位置：`backend/`（Python + FastAPI + LangChain/LangGraph）、`frontend/`（React + TS + Vite）

> 阅读约定：本文中「已验证」指有自动化测试或真实服务运行记录；没有实测的一律写作「未执行」，不写推测性结论。所有模型能力相关的成绩均使用确定性替身模型产生，**不代表真实模型成绩**。

---

## 1. 本次交付的一句话总结

让设计文档里的 Harness 从「架构图」变成「能跑、能解释、能复现异常恢复」的系统：真实导入 → 真实向量化 → 真实混合检索 → 真实 `create_agent` 循环 → 真实审批中断与恢复对账 → 真实产物下载，配套 99 项自动化测试、统一命令入口和中文工作台。

---

## 2. 交付物清单

### 2.1 后端（38 个源文件）

```text
backend/
  pyproject.toml                 # 依赖与工具配置（uv 锁定）
  uv.lock                        # 实测版本组合
  src/harnesslab/
    config.py                    # 三层配置：默认值 < configs/*.json < 环境变量；脱敏摘要
    errors.py                    # 文档 05 第 7 节错误码契约
    utils.py                     # 时间/ID/哈希/token 估算
    cli.py                       # doctor / db migrate / api serve / worker serve /
                                 # demo seed / index rebuild / eval run / data export|restore
    demo.py                      # 演示资料幂等导入
    api/
      app.py                     # 应用装配、统一错误处理、单端口托管前端
      deps.py                    # 本机开发身份 + 项目作用域校验
      routes_health.py           # 健康、模型档案与真实探测
      routes_projects.py         # 项目、文档、检索实验室、索引重建、任务、记忆、评测
      routes_runs.py             # 会话、运行、SSE、取消/重跑/分叉、审批、产物、原文
      sse.py                     # 事件流（补播、心跳、seq 去重）
    runtime/
      worker.py                  # 领取任务、租约协调、后台任务（导入/重建/评测）
      executor.py                # 单次运行执行器（上下文→计划→图→审批中断→校验→终态）
    agents/
      factory.py                 # create_agent + 文件检查点 + 有限规模计划
      prompts.py                 # Prompt 版本库 + 受控 Skills 加载
    harness/
      context.py                 # 装配顺序、不可信数据块包装、压缩保真规则
      budget.py                  # 模型/工具/时长/token 预算 + 循环检测
      policy.py                  # 服务端权威策略表与风险分类
      models.py                  # Chat 模型工厂 + 流式/工具调用能力探测
    knowledge/
      loader.py                  # Markdown / TXT / 文本 PDF 解析（保留页码与字符偏移）
      chunker.py                 # 600 token / 80 重叠，超长段强制切分
      embeddings.py              # 自研 LM Studio Embedding 适配器 + 索引指纹
      index.py                   # Qdrant Local，统一项目 + 索引版本过滤
      retrieval.py               # jieba 分词 + BM25 + RRF
      service.py                 # 导入流水线、检索、引用校验、删除、索引重建
    tools/
      base.py / local.py         # 工具上下文与 7 个工具实现
      registry.py                # 包装为 LangChain 工具，统一经网关
      gateway.py                 # 策略→预算→审批→账本→审计
      workspace.py               # 受限工作区与路径越界防护
    storage/
      db.py                      # 业务库 schema 与迁移（与检查点分库）
      models.py                  # 状态机枚举、预算、计划、引用、工具返回结构
      repositories.py            # 幂等、租约 CAS、同 thread 互斥、审批版本、工具账本
    observability/               # 脱敏结构化日志 + 事件出口
    evals/runner.py              # 检索质量评测与报告
  tests/                         # 8 个测试文件，99 个用例
```

### 2.2 前端（16 个源文件）

`概览`、`任务工作台`（三栏：消息/引用/计划 + 执行详情/用量/时间线 + 审批卡）、`知识库`（上传、版本、检索实验室）、`审批中心`、`评测中心`、`设置`。

### 2.3 工程外围

- `configs/app.config.json`：无密钥配置层
- `prompts/system.md`、`prompts/plan.md`：版本化 Prompt
- `skills/technical-report/`：受控技能（含报告模板），声明式 `required_tools` 不提升权限
- `datasets/demo/`：5 份合成资料 + `gold.json`（含故意植入的提示注入文本与虚构容量参数）
- `scripts/`：`probe_lm_studio.ps1`（独立连通性检查）、`smoke_e2e.ps1`（端到端烟雾测试）、`dev.ps1`（一键开发）
- `.env.example`、`.gitignore`、`README.md`（运行步骤 + H01–H25 能力状态表 + 排障速查）

---

## 3. 需求覆盖矩阵（对文档 01 的 H01–H25）

| 需求 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| H01 模型接入与能力探测 | 已实现，**真机未探测** | `doctor --probe` 与 `POST /model-profiles/{id}/probe` 会真实调用 `/models`、`/chat/completions` 与工具探测；本机 LM Studio 实测记录尚未填回文档 11 |
| H02 Agent 循环与结构化结果 | 已实现并验证 | 脚本化替身驱动 `create_agent`，完成「检索 + 计算」两次工具调用；最终答复经 `AgentAnswer` Schema 校验，失败保留原文并标注限制 |
| H03 会话与短期记忆 | 已实现 | thread 状态 + `SqliteSaver` 文件检查点；同 thread 只允许一个非终态 run（DB 约束 + 用例） |
| H04 工具注册与输入输出校验 | 已实现并验证 | LangChain 签名校验先于网关；非法参数无副作用（用例覆盖） |
| H05 RAG 与来源引用 | 已实现并验证 | 5 份资料 → 24 chunk；引用标签映射到固定文档版本与页码/字符区间；重复导入复用版本；删除后立即阻断检索与原文 |
| H06 持久化与恢复 | 已实现并验证（替代故障注入） | 租约 CAS + fencing token；过期租约进入 `recovering`；结果不明的副作用进入人工核对且不自动重试 |
| H07 流式事件与取消 | 已实现 | 先落库后发送、`(run_id, seq)` 唯一、`Last-Event-ID` 补播、游标过旧返回 410；取消在模型/工具边界检查 |
| H08 预算 | 已实现并验证 | 模型 20 / 工具 30 / 活跃 180s / 单工具 30s / 重试 2 / 重规划 2 / token 预算；耗尽落 `BUDGET_EXCEEDED` 并保留已有产物 |
| H09 可观测性与评测 | 已实现 | 事件表 + 运行时间线 + 配置快照（模型、索引、Prompt、技能、工具版本）；检索评测输出 Recall@k / MRR / 无答案处理 |
| H10 计划与重规划 | **部分** | 计划生成、步骤状态、`plan.updated` 事件已实现；重规划仅有上限配置，未接线 |
| H11 人工审批与参数修改 | 已实现并验证 | `interrupt()` 中断 + 审批实体落库；批准/拒绝/修订版本/过期/双批准冲突/取消后批准均有用例；批准后只产生一份工单 |
| H12 上下文压缩 | 已实现，**质量回归未执行** | 装配顺序 + 80% 阈值压缩；保留用户约束、审批状态、未完成步骤、产物与引用 ID |
| H13 长期记忆 | 已实现并验证 | 候选/确认/修订/删除 + revision 冲突；删除立即阻断读取 |
| H14 混合检索与重排 | **部分** | 向量 + 中文 BM25 + RRF + 检索实验室已实现；重排模型未接入 |
| H15 Skills 与 Prompt 版本 | 已实现 | `SKILL.md` 加载、内容哈希进入 run 快照；`required_tools` 不能提升权限（用例覆盖） |
| H16 MCP | 未实现（P1） | — |
| H17 受限工作区与产物 | 已实现并验证 | 绝对路径、盘符、UNC、`..`、备用数据流、受限设备名、符号链接逃逸全部拒绝；产物按 ID 下载，HTML 强制附件 |
| H18 多 Agent | 未实现（P1） | 子任务协议 `Findings` 已定义 |
| H19 权限与注入防护 | **部分** | 服务端权威策略、未知工具默认拒绝、注入文本按不可信块处理并写入限制项；对抗回归未执行 |
| H20 回放与评测比较 | **部分** | 时间线读取、重跑（新 run）、检查点分叉、检索对照已实现；回放页与配置对比页未实现 |
| H21–H25 | 未实现（P2） | 沙箱关闭时后端直接拒绝 `run_python_sandbox`，界面不提供入口 |

---

## 4. 关键实现说明

### 4.1 运行生命周期（文档 03 第 2、3 节）

`queued → running → waiting_approval → queued → … → completed/failed/cancelled`。所有对外可见状态先写业务表与事件表，再更新 run 状态，避免出现「已完成但无产物」的中间态。`waiting_approval` 释放执行槽位并保留检查点，等待时间不计入活跃时长但有独立过期时间（默认 24h）。

### 4.2 审批中断的真实链路

```
模型请求 create_demo_ticket
  → 工具网关按策略判定 REQUIRE_APPROVAL
  → 工具函数内 interrupt({tool_name, 规范化参数, 参数哈希, 预期影响, tool_call_id})
  → 执行器捕获 __interrupt__，写入 approvals（含 checkpoint_id）+ 发 approval.requested
  → run 转 waiting_approval，工作进程释放
用户批准（API 校验 revision + 参数哈希 + 有效期 + run 状态）
  → run 重新入队并带 resume 载荷
  → 执行器 Command(resume=...) 恢复图，工具继续执行，幂等键命中账本
```

工具账本幂等键 = `run_id + 逻辑动作 ID(tool_call_id) + 工具版本 + 审批参数版本`；**崩溃发生在「工单已提交、图节点未提交」边界时，恢复会复用已持久化结果，不会产生第二份工单**（已用测试固定）。

### 4.3 与框架的边界（ADR 实现期补充）

1. 审批不使用 `HumanInTheLoopMiddleware`：业务表需要记录参数哈希与修订版本，框架通用中断无法表达「一次批准只绑定一个参数版本」，两套并用会产生互不知晓的审批记录。
2. `create_agent` 编译图直接作为运行图；队列、租约、状态机、审批、恢复对账在 `runtime` 层显式实现，避免内外双层模型/工具循环冲突。代价是计划/收尾不在图检查点内（计划无副作用，重算安全）。
3. `RunExecutor` 通过 `model_factory` 注入模型，使控制流测试可用确定性替身，而生产使用真实 Chat 模型。

### 4.4 单进程约束的工程处理

Qdrant Local 只允许单进程访问目录，因此 `api serve` 默认内联工作进程（`RUN_WORKER_IN_API=true`），并在启动日志与 README 中说明：要独立 `worker serve`，需把 `VECTOR_MODE` 切到 `service`。这是对文档 02「API 不另开实例竞争该目录」的落地方式。

### 4.5 关于「不虚构精确性」

- 无本地 tokenizer → 保守公式估算，结果标注 `token_estimate_kind: estimate`
- 价格不可用 → 显示「费用：未知」，不填零
- 探测失败 → 能力字段保持 `false` 并给出原因，不假装可用
- 评测报告中生成质量、任务成功率等未跑项 → 写「未执行」而不是留空

---

## 5. 验证记录

### 5.1 静态检查

```
$ uv run ruff check src tests
All checks passed!
```

规则集显式声明（`E,F,I,UP,B,SIM,RUF`），并关闭会误报中文全角标点的 `RUF001/002/003`。

### 5.2 自动化测试

```
$ uv run pytest
99 passed in 5.53s
```

| 测试文件 | 用例数 | 覆盖重点 |
| --- | --- | --- |
| `test_units.py` | 29 | 路径越界 9 种写法、受限计算器 5 种绕过尝试、预算与循环检测、策略裁决与技能不越权、上下文压缩保真、不可信块标注 |
| `test_repositories.py` | 12 | run 幂等与 409、同 thread 互斥、租约 CAS 与续约、过期租约恢复、审批双批准冲突、审批修订失效、审批过期、工具账本、工单唯一、事件 seq |
| `test_knowledge.py` | 12 | 导入就绪与定位、重复导入复用、二进制转不支持、混合/向量检索、无答案、引用解析与非法标签、删除阻断、索引指纹不符拒绝 |
| `test_gateway.py` | 9 | 未知工具拒绝、副作用幂等复用、结果不明转人工核对、拒绝无副作用、产物与下载引用、工具超时、project_id 参数被忽略、预算耗尽、取消前置检查 |
| `test_executor.py` | 12 | 完成含引用、非法引用标签、无证据标注、预算耗尽保留产物、审批中断→批准→只一份工单、拒绝不建工单、恢复时结果不明进人工核对、取消保留结果、报告产物可下载、工具失败不中断运行、终态保护 |
| `test_worker.py` | 5 | 后台导入任务、失败任务、领取执行、排队取消、租约过期恢复 |
| `test_api.py` | 21 | 健康、脱敏档案、策略快照、上传导入与预览、演示导入幂等、上传校验、运行幂等与 409、预算只能降低、事件与游标、取消/重跑/分叉、审批决策与冲突、参数修订、产物下载与越界 404、原文定位、记忆修订冲突、评测任务 |

### 5.3 前端

```
$ npm run build
tsc --noEmit && vite build
41 modules transformed
dist/assets/index-*.js  269.01 kB │ gzip: 83.69 kB
✓ built in 799ms
```

### 5.4 真实服务端到端（替身模型）

```
$ pwsh scripts/smoke_e2e.ps1 -Port 8123
1/6 服务存活  就绪状态：ready
2/6 创建项目  排队导入 5 份演示资料
3/6 索引完成：24 个 chunk，维度 64
4/6 检索命中 5 条，模式 hybrid，索引 73355043
5/6 提交运行（queued）
6/6 运行终态：completed ｜ 事件数 6 ｜ 用量 模型 1 次 / 工具 0 次
```

（替身 Chat 模型不发起工具调用，故工具 0 次、引用 0 条并自动追加两条限制说明——这正是「未检索到证据/未给出引用」的诚实标注路径。）

### 5.5 浏览器端走查

Chrome + 真实后端，替身模型：

- 概览 / 任务工作台 / 审批中心三页正常渲染，**控制台 0 错误**
- 任务工作台正确展示：用户提问、助手答复、限制说明、执行计划、引用区、运行操作（取消/重跑/分叉）、用量进度条、执行时间线（入队→开始→计划→预算→消息完成→运行完成）
- 审批中心展示真实规范化参数、参数哈希、有效期与三项操作；点击「修改参数」提交后生成 **v2 待审批版本**，v1 自动变为 `superseded`（已用 API 复核）

### 5.6 未执行的验证（明确记录）

- 真实 LM Studio Embedding 的模型 ID、revision、实测维度、中文召回质量
- 真实 Chat 模型的工具调用稳定性与多轮对话（审批链路当前由脚本化替身驱动验证）
- 进程 kill 级故障演练（当前用「租约过期 + 账本状态」等价注入）
- 注入与越权对抗回归、跨项目 ID 伪造、恶意 MCP 描述
- 性能指标：运行创建 p95、首个状态事件、首 Token、端到端耗时
- 100 条评测集、三组对照实验、两次演示彩排

---

## 6. 已知限制

1. **单机单进程**：单工作进程、SQLite、Qdrant Local；不承诺多工作进程共享本地向量目录。服务化迁移需数据导出校验、索引重建、检查点版本兼容与恢复演练，不只是替换连接串。
2. **计划与收尾不在图检查点内**：崩溃恢复时计划会重算（无副作用），但不会恢复「已完成步骤列表」的历史版本。
3. **重规划、重排、MCP、多 Agent、回放页未实现**，不能宣称支持。
4. **注入防护是辅助而非保证**：系统提示与不可信块标注已实现，模型是否被绕过需要对抗测试来度量。
5. **本机开发身份**：仅绑定 loopback；进入局域网/公网前必须实现正式认证与资源授权。

---

## 7. 建议的后续任务（可直接转 Issue）

### M3 收尾（补齐单 Agent 完整体验）

- [ ] T301b 把重规划接线到执行器：工具失败后按 `max_replans` 触发一次新的计划生成，并记录 `plan.version`
- [ ] T302b 审批中心支持「按 run 聚合」视图与决策审计导出
- [ ] T305b 上下文压缩质量回归：固定样本对比压缩前后对用户约束/引用定位的保留率
- [ ] T303b 增加真实进程 kill 故障演练脚本（`scripts/crash_inject.ps1`），并记录恢复耗时

### M4 扩展展示

- [ ] T401b 可选重排：先做能力探测与资源测量，缺失时明确展示「使用融合排序」
- [ ] T402b 技能版本快照进入评测报告，形成「技能版本 → 质量」的对照记录
- [ ] T403 本地只读 MCP 服务 + 适配器（含恶意描述与超时测试）
- [ ] T404 研究者/核查者/写作者子图 + 父子预算共享与取消传播
- [ ] T405 回放页（禁用工具执行）与检查点分叉的界面区分

### M0 待填实测记录

- [ ] 用 `scripts/probe_lm_studio.ps1` 与 `harnesslab doctor --probe` 采集真实模型档案，回填 `docs/11` 的「待填实测记录」
- [ ] 若 Chat 模型不支持工具调用，记录为「受模型限制」并保留 `CHAT_PROVIDER=stub` 的降级演示路径

---

## 8. 风险与排障提示

| 现象 | 原因与处理 |
| --- | --- |
| `INDEX_VERSION_MISMATCH` | 更换 Embedding 模型/前缀导致指纹变化 → `harnesslab index rebuild`，不要清空数据 |
| 向量库被占用 | 同时运行了 `api serve`（内联工作进程）与 `worker serve` |
| 审批点不动 | 检查 `approval_ttl_hours` 是否过期、`revision`/参数哈希是否与当前版本一致 |
| 反复出现同一工具调用 | 触发循环检测（相同工具+参数连续三次无新结果） |
| 页面消息重复 | 客户端必须按 `seq` 去重，重连携带 `Last-Event-ID` |
| `.ps1` 脚本报语法错误 | 用 PowerShell Core（`pwsh`）执行，5.1 会按 ANSI 解码中文脚本 |

---

## 9. 变更基线

| 项目 | 值 |
| --- | --- |
| 设计文档 | `docs/01`～`docs/11`（2026-09-13） |
| Python | 3.12.8（设计文档写 3.11，本机实际 3.12） |
| 关键依赖 | langchain 1.4.0 · langgraph 1.2.11 · langchain-openai 1.6.2 · langgraph-checkpoint-sqlite 3.1.1 · qdrant-client 1.19.0 · fastapi（starlette 1.6.0） · React 19 · Vite 7 |
| 依赖锁 | `backend/uv.lock`、`frontend/package-lock.json` |
| 测试 | 99 项通过；ruff 全绿；前端类型检查与构建通过 |

升级依赖后必须重跑：图恢复、消息序列化、Embedding 适配、审批竞态四类测试，并保留旧版本工作进程处理未完成任务。
